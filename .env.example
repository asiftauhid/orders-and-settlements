# MongoDB Atlas connection string.
# Get this from Atlas: Database > Connect > Drivers > Node.js.
MONGODB_URI=

# Used by Auth.js to sign/verify session JWTs. Generate with: openssl rand -base64 32
AUTH_SECRET=
